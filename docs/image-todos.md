# Image todos

Images the site still needs or should replace. Follow the house style and the
conventions in [`assets/img/README.md`](../assets/img/README.md).

## Journal plate should drop the paper

`assets/img/journal.jpg` (+ `-dawn`) currently shows an open paper notebook with
handwritten notes. Kasper does not use paper; thoughts are kept in a notes app.
Replace the plate with a scene that does not lean on a paper notebook (for
example a lit phone on the café table beside the cooling tea), and update the
caption "the notesbog, before transcription" in `journal/index.html`, which also
implies handwriting transcribed to digital.
